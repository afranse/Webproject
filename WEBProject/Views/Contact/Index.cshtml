@model PageContent
<partial name="_T15contact" model="@Model.Pages[0]" />
<partial name="_HowToContacts" />
@for (int i = 0; i < 3; i++)
{
    <div class="row contactform">
        @for (int x = 0; x < 3; x++)
        {
            <div class="col-md-4 ">
                <partial name="_MultipleContact" model="@ViewBag.Contact" />
            </div>
        }
    </div>
}
